{
  "devDependencies": {
    "cypress": "^13.15.0",
    "cypress-axe": "^1.5.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "merge-reports": "mochawesome-merge cypress/reports/*.json > merged-report.json && npx mochawesome-report-generator merged-report.json"
  }
}
